use crate::buffer::Buffer;

/// A window that contains a buffer.
pub struct Window {
    /// Window buffer.
    pub buffer: Buffer,
}

impl Window {
    pub fn new() -> Self {
        todo!()
    }
}
